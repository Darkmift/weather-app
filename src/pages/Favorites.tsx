import { Container } from '@mui/material';

const Favorites = () => {
  return <Container sx={{ display: 'flex', flexDirection: 'column', gap: 5 }}>Favorites</Container>;
};

export default Favorites;
